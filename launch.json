{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Chrome",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:5500", // Change this to match your local development server URL
            "webRoot": "${workspaceFolder}"
        }
    ]
}
